{
  "name": "test-git-action",
  "version": "5.0.4",
  "main": "index.js",
  "type": "module",
  "license": "ISC",
  "scripts": {
    "build": "mkdir -p dist && cp -R node_modules action.yml index.js LICENSE package.json package-lock.json README.md dist"
  },
  "dependencies": {
    "@actions/core": "^1.10.0",
    "@octokit/rest": "^19.0.43"
  }
}
